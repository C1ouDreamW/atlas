# 项目待办清单 (TODO List)

## 总结
**阶段三（用户鉴权与上下文体系）已全部完成。** 项目中不再有任何硬编码 `userId = 1L`，Controller 层已统一通过 `UserContextHolder.get()` 从 JWT 拦截器注入的线程上下文中获取当前登录用户 ID。

当前阶段四已完成，阶段五（刷题与错题本）核心接口已落地；后续重点集中在 Controller/集成测试、压测报告与容器化部署。

## 详细待办列表

## 已完成特性（补充 plans_1.md 之外的新增功能）

- [x] **题库大厅**：`GET /api/v1/question-banks/public` — 分页查询所有公开题库（`is_public=1`），按更新时间降序，无需登录。已在 `WebMvcConfig` 中白名单放行。

## 业务与架构开发待办 (基于 plans_1.md)

以下是从项目规划文档中整理出的各个阶段尚未完成的核心业务和技术任务：

### 阶段三：用户鉴权与基础业务开发
- [x] **完善用户模块**：编写 `UserService` 与 `UserController`，实现基于 BCrypt 的用户密码加密存储与校验及注册功能。
- [x] **集成 JWT 与安全鉴权**：实现 JWT 签发与解析逻辑；编写 Spring `Interceptor` 或 `Filter` 实现接口的安全鉴权。

### 阶段四：AI 智能题库导入模块 ✅
- [x] **文档解析工具**：引入并集成 `PDFBox` 和 `Apache POI` 工具类，实现 PDF/Word 文档的纯文本抽取。
- [x] **Redis Stream 任务体系**：生产者/消费者解耦，任务 5 态流转（SUBMITTED→PROCESSING→PARSED→IMPORTING→IMPORTED）。
- [x] **预览→确认→幂等落库**：沙盒草稿箱机制，用户预览 AI 解析结果后确认导入，同一个 taskId 通过 Redis SETNX 防止重复落库。
- [x] **文件上传端点**：`POST /api/v1/ai-import/submit`，支持上传文件返回 taskId，`GET /tasks/{taskId}/status` 轮询进度。
- [x] **限流防护**：AiImportRateLimiter，基于 Redis Lua 脚本的滑动窗口计数器，默认 5 次/小时/用户。
- [x] **超时清理**：AiImportTaskWatchdog，@Scheduled 每 2 分钟扫描长期 PROCESSING 任务，超 10 分钟自动标记 FAILED。

### 阶段五：高并发刷题与错题本模块
- [x] **刷题业务逻辑**：已新增 `PracticeController` / `PracticeService`，支持按题库拉取刷题题目（隐藏答案与解析）、顺序/随机刷题，以及提交单题答案自动判分。
- [x] **客观题判分与错题自动收集**：`PracticeServiceImpl` 已支持 `SINGLE` / `MULTI` / `JUDGE` 自动判分；答错后调用 `WrongQuestionService.recordWrong()` 自动写入错题本；主观题或未知题型返回 `needsManualGrading=true`，不自动入错题本。
- [x] **错题本业务闭环**：已新增 `WrongQuestionService` / `WrongQuestionController`，支持错题首次插入、重复做错递增次数、移除后再次做错复活、分页查看错题本、按错题本重刷。
- [x] **刷题读路径复用 Redis 热点缓存**：公开题库刷题列表复用 `QuestionBankHotDetailService` 的 Cache-Aside 热点缓存；私有题库仍执行归属校验后直接查询 DB。当前未额外引入 Spring Cache 或 Redisson。
- [x] **阶段五 Service 单元测试**：已新增 `PracticeServiceImplTest` 和 `WrongQuestionServiceImplTest`，覆盖公开/私有题库拉题、客观题判分、错题写入/复活/移除等核心路径。
- [ ] **阶段五接口层与边界测试补齐**：补充 `PracticeController`、`WrongQuestionController` 的 Web 层测试或集成测试，覆盖 JWT 上下文、Validation、统一响应和异常映射。
- [ ] **错题分页查询优化**：当前 `bankId` 过滤在 Service 内按分页结果做 Java 层过滤，适合错题量有限场景；后续可改为自定义 SQL/JOIN，保证按题库过滤时的全量分页与 `total` 精确性。

### 阶段六：测试、压测与部署上线
- [ ] **单元测试配置**：为现有的核心 Controller 与 Service 编写高覆盖率的单元测试。
- [ ] **接口压力测试**：使用 JMeter或Apifox 对核心高频接口（例如有无Redis缓存对比）进行压测，并输出报告留存。
- [ ] **容器化与部署**：在项目根目录编写 `Dockerfile` 与 `docker-compose.yml`，以支持后端服务、MySQL、Redis的一键部署。

## 后续建议 (技术重构)
1. **双 Token 机制**：引入 Access Token + Refresh Token，配合 Redis 黑名单实现 Token 主动失效与踢下线。
2. **权限后续增强**：三级角色 `USER` / `PREMIUM` / `ADMIN` 已落地，后续可补充角色变更审计、Token 主动失效与管理端操作审计。