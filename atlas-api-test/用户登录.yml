info:
  name: 用户登录
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:8080/api/v1/users/login
  body:
    type: json
    data: |-
      {
        "username":"zhangsan",
        "password":"123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
