info:
  name: 分页查询公开题库（题库大厅）
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/api/v1/question-banks/public?current=1&pageSize=10
  params:
    - name: current
      value: "1"
      type: query
    - name: pageSize
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
