info:
  name: 根据题目ID查找
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/api/v1/questions/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
