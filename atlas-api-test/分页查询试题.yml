info:
  name: 分页查询试题
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/api/v1/question-banks/1/questions?current=1&pageSize=10&keyword=
  params:
    - name: current
      value: "1"
      type: query
    - name: pageSize
      value: "10"
      type: query
    - name: keyword
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
